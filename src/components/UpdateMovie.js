import React from "react";

const UpdateMovie = () => {
  /*
    This component should display a form to update a movie
    - this is a dynamic route so you will need to get the movie id from the params using (useParams)
    - create a GET request using axios to the backend server (/api/movies/:id)
    - preﬁll the form with the old movie details
    - on submit create a PUT request using axios to (/api/movies/:id)
    - if the request was successful navigate back to the movies page
    */
  return <div>UpdateMovie</div>;
};

export default UpdateMovie;
